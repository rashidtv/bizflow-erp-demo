services:
  - type: web
    name: bizflow-frontend
    env: static
    plan: free
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://bizflow-erp-backend.onrender.com/api